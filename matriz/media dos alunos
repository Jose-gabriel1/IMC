//ex.1 (media de alunos)
/*
#include <iostream>
#include <ctime>
#include <cstdlib>
using namespace std;

int main() {
    system("color a");

    int nota1[10], nota2[10], i;
    float media[10];
    srand(time(0));

    for(i = 0; i < 10; i++) {
        nota1[i] = rand() % 11;
        nota2[i] = rand() % 11;
        media[i] = (nota1[i] + nota2[i]) / 2.0;

        if (media[i] >= 7) {
            cout << "Aluno " << i+1 << ": Nota1 = " << nota1[i] << ", Nota2 = " << nota2[i] << ", Media = " << media[i] << ", Situacao: Aprovado" << endl;
        } else if (media[i] >= 3) {
            cout << "Aluno " << i+1 << ": Nota1 = " << nota1[i] << ", Nota2 = " << nota2[i] << ", Media = " << media[i] << ", Situacao: Na final" << endl;
        } else {
            cout << "Aluno " << i+1 << ": Nota1 = " << nota1[i] << ", Nota2 = " << nota2[i] << ", Media = " << media[i] << ", Situacao: Reprovado" << endl;
        }
    }

    return 0;
}
*/
