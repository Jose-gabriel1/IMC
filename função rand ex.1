#include <ctime>
#include <unistd.h>
#include <cmath>

using namespace std;



int main() {
system("color a");

    srand(time(0));

    int i = 1, n;

    n = rand()%1;
    cout << n << "\n";
    n = rand()%2;
    cout << n << "\n";
    n = rand()%3;
    cout << n << "\n";
    n = rand()%10;
    cout << n << "\n";
    cout << "i\tn\n" ;

    while(i <= 5){

        n = rand()%10;
        cout << i << "\t" << n << "\n";
        sleep(2);
        i = i + 1;


    }

return 0;
}

// a <= n <= b // n = a + rand()%x; // Onde x = b - a + 1 // 33 <= n <= 67 // n = 33 + rand()%35; // Onde x = b - a + 1 = 67-33+1=35
