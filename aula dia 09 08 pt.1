#include <iostream>
#include <ctime>
#include <cstdlib>
#include <locale.h>
using namespace std;

int main () {

    setlocale(LC_ALL,"Portuguese");
    srand(time(0));
    system ("color 2");

    int i, n, a = 0, b = 0, c = 0, d = 0, e = 0, f= 0;

  for(i=1; i <= 6000; i++) {

    n = 1+rand()%6;
    cout << n << "\n";
    if(n == 1){
        a++;
    }
    if(n == 2){
        b++;
    }
    if(n == 3){
        c++;
    }
    if(n == 4){
        d++;
    }
    if(n == 5){
        e++;
    }
    if(n == 6){
        f++;
    }

   }
   cout << "o número 1 aparece " << a << " vez(es) " << endl;
   cout << "o número 2 aparece " << b << " vez(es) " << endl;
   cout << "o número 3 aparece " << c << " vez(es) " << endl;
   cout << "o número 4 aparece " << d << " vez(es) " << endl;
   cout << "o número 5 aparece " << e << " vez(es) " << endl;
   cout << "o número 6 aparece " << f << " vez(es) " << endl;


   return 0;

}
